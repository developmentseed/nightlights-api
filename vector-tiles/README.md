# Vector Tile Sources

## Admin Boundaries

`india-lights-boundaries.tm2source` is a Mapbox Studio source that's designed
to pull from the boundaries in the API database.  It currently targets
`localhost`.  If Studio doesn't let you change that, it can be set by editing
`data.yml` and `data.xml`.
